#include <iostream>
#include <cstring>
#include <cmath>
using namespace std ;

int f (string s1 , string s2) {
            int a = s1.length () , b = s2.length () ;
            if (a == b) {
                        cout << 0 << "\n" ;    
            } else if (a > b) {
                        cout << 1 << "\n" ;
            } else {
                        cout << -1 << "\n" ;
            }
            return 1 ;
}

int main () {
            string s1 , s2 ;
            cin >> s1 ;
            cin >> s2 ;
            f (s1 , s2) ;
            return 0 ;
}
